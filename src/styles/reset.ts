import { css } from '@emotion/react';

const reset = css`
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    button {
        border: 0;
        background-color: transparent;
    }
`;

export default reset;
